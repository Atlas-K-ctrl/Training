# Atlas Training

Deployable GitHub Pages + Supabase version.

Files:
- index.html — app
- manifest.webmanifest — installable PWA manifest
- service-worker.js — offline cache
- icon-192.png / icon-512.png — app icons
- supabase_setup.sql — database + security policies

Supabase is already configured in index.html with the project URL and publishable browser key. The database schema and RLS policies have also been applied.
